@using scaffold.Models

<h1>Wall</h1>
<h3>Post a Message!</h3>
<div>
    @Html.Partial("AddMessage")
</div>
@{
    foreach (var message in ViewBag.Messages)
    {
        CommentViewModel commentForm = new CommentViewModel();
        commentForm.message_id = message.id;
        <br>
        <p>"@message.body"</p>
        <p class="text-primary">-@message.user.first_name @message.updated_date</p>
        @Html.Partial("AddComment", commentForm)
        <hr>
    }
}